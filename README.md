# framer-task

A modern, animated task manager built with **React** and **Framer Motion**. Track your tasks by adding, completing or marking them as failed—with smooth animations and component reusability built in.

## 🎯 Features

- Add new tasks with ease  
- Mark tasks as **Completed** or **Failed**  
- View a list of tasks and their current status  
- Animated transitions using Framer Motion for a fluid UX  
- Built with React functional components & hooks (state management, props, event handling)  
- Responsive layout, clean code with component structure  

## 🧰 Technologies Used

- **React** (JSX + Hooks)  
- **Framer Motion** for animations  
- **Vite** (or your build tool) for fast development  
- **CSS / SCSS** (or styling method you used)  
- (Optional) LocalStorage or simple state persistence if implemented  

## 🚀 Getting Started

### Prerequisites  
Make sure you have Node.js and npm installed.  
```bash
node -v
npm -v
```bash
git clone https://github.com/Jayanth-shetty/framer-task.git
cd framer-task
npm install
```
```bash
npm run dev
```
